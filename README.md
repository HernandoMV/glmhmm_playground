* Create a new conda environment if you want with python 3.11

* Install glmhmmt:
```
uv pip install glmhmmt
```

* Create a new notebook, synchronizing between the browser and VScode:
```
uv run marimo edit play.py --watch
```
